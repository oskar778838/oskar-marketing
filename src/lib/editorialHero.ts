// Editorial Hero — replaces the lamellae-spiral 3D centerpiece.
// Phase 1: stub. Phase 2 fills char-by-char SplitType reveal.
// Phase 3 wires ScrollTrigger pin + Lenis bridge + scrubbed choreography.

import type Lenis from "lenis";

export function initEditorialHero(_lenis: Lenis | null): void {
  // Implementation lands in Phase 2 (reveal) + Phase 3 (pin/scrub).
  // Left empty so main.ts/pro.ts compile cleanly after the 3D removal.
}
