node_modules
dist
.DS_Store
*.log
.vite
.env
.env.local

# Claude Code local config (incl. settings.json with bypassPermissions for dev)
.claude/

# HDRI environment map (1.5MB) — used only for local quality testing.
# Production uses programmatic RoomEnvironment for env reflections.
public/hdri/

# Patch files (used as transient handoffs, never committed)
*.patch
outputs/

# Ultra-review generated audit output (Lighthouse/axe JSON, screenshots).
# The human-readable summary lives in REVIEW-REPORT.md, which IS committed.
.review-artifacts/
scripts/diagnose-output.json
